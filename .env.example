# Copy to .env and fill in real values. NEVER commit .env.

# Django (generate with:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
SECRET_KEY="a8vmn8-kshjv!q8qs!7*h4-k1u43sp635gigjgdq+8sttvi0x+"

# Stripe (from https://dashboard.stripe.com/apikeys and the webhook endpoint settings)
STRIPE_SECRET_KEY=a8vmn8-kshjv!q8qs!7*h4-k1u43sp635gigjgdq+8sttvi0x+
WEBHOOK_KEY=

# Google OAuth (django-allauth)
CLIENT_ID_GOOGLE=
SECRET=

# Email (SMTP)
EMAIL_HOST_PASSWORD=
